# Antigravity App

## Record

Add `--output-dir "<active-conversation-artifact-directory>"` to the recording
command. Use the exact physical directory supplied by Antigravity App.

## Deliver

Render the returned absolute `path` directly as native audio on its own line
above the written response:

```markdown
![audio](<$path>)
```
